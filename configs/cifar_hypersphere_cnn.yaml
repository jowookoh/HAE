name: cifar_hypersphere_cnn

dataset:
  name: cifar10
  img_size: 32
  channels: 3

model:
  encoder: cifar
  decoder: cifar_cnn
  bottleneck: hypersphere
  latent_dim: 256

training:
  epochs: 50
  batch_size: 64
  lr: 0.0001
  kl_weight: 0.001
  visualize_every: 5
